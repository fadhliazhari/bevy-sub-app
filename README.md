# bevy-sub-app
A minimal implementation of bevy and bevy sub app

This is a sample bevy application that utilize bevy sub app functionality.
It is utilizing a `String` `VecDeque` to pass the data between app and sub app.
